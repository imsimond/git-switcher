{
  "landingPage": "/wp-admin/plugins.php",
  "login": true,
  "steps": [
    {
      "step": "installPlugin",
      "pluginData": {
        "resource": "git:directory",
        "url": "https://github.com/imsimond/git-switcher",
        "ref": "main"
      }
    },
    {
      "step": "runPHP",
      "code": "<?php require_once '/wordpress/wp-load.php'; $path = ABSPATH . 'wp-content/plugins/debug-bar'; if (!file_exists($path)) { exec(\"git clone https://github.com/WordPress/debug-bar.git $path && cd $path && git checkout HEAD~1\"); } ?>"
    }
  ]
}
